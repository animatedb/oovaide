# Generated by oovCMaker
# - Config file for the Oovcde package
# Compute paths
get_filename_component(OOVCDE_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(OOVCDE_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET OovcdeAND NOT OOVCDE_BINARY_DIR)
   include("${FOOBAR_CMAKE_DIR}/OovcdeTargets.cmake")
endif()

# These are IMPORTED targets created by OovcdeTargets.cmake
set(OOVCDE_LIBRARIES oovCommon oovGuiCommon)
set(OOVCDE_EXECUTABLE ClangView oovBuilder oovCMaker oovCovInstr oovCppParser oovEdit oovcde)

