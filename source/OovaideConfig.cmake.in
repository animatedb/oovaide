# Generated by oovCMaker
# - Config file for the Oovaide package
# Compute paths
get_filename_component(OOVAIDE_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(OOVAIDE_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
if(NOT TARGET OovaideAND NOT OOVAIDE_BINARY_DIR)
   include("${FOOBAR_CMAKE_DIR}/OovaideTargets.cmake")
endif()

# These are IMPORTED targets created by OovaideTargets.cmake
set(OOVAIDE_LIBRARIES oovCommon oovGuiCommon)
set(OOVAIDE_EXECUTABLE ClangView oovBuilder oovCMaker oovCovInstr oovCppParser oovEdit oovaide)

