# Generated by oovCMaker
add_executable(ClangView CLangView.cpp)

target_link_libraries(ClangView oovCommon oovGuiCommon ${GTK_LIBRARIES} 
  ${LLVM_LIBRARIES})

install(TARGETS ClangView
  EXPORT OovaideTargets
  RUNTIME DESTINATION "${INSTALL_BIN_DIR}" COMPONENT lib)
